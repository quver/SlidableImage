// swift-tools-version:5.0

import PackageDescription

let package = Package(
    name: "SlidableImage",
    products: [
        .library(
            name: "SlidableImage",
            targets: ["SlidableImage"]),
    ],
    dependencies: [
    ],
    targets: [
        .target(
            name: "SlidableImage",
            dependencies: []),
    ]
)
