platform :ios, '9.0'

target 'SlidableImage' do
  use_frameworks!

  target 'SlidableImageTests' do
    inherit! :search_paths
    pod 'iOSSnapshotTestCase'
    pod 'Nimble'
    pod 'Quick'
  end

end
